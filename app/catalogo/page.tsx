import { CatalogClient } from '@/components/catalogo/CatalogClient';

export const metadata = {
  title: 'Catálogo | Tupã Sports'
};

export default function CatalogPage() {
  return <CatalogClient />;
}
